import { RouteSkeleton } from "@/components/global/route-skeleton"

export default function CartPage() {
	return (
		<RouteSkeleton
			title="Your Cart"
			description="Cart screen is scaffolded for basket review, quantity updates, and checkout progression."
		/>
	)
}
