import { RouteSkeleton } from "@/components/global/route-skeleton"

export default function CheckoutPage() {
	return (
		<RouteSkeleton
			title="Checkout"
			description="Checkout route exists as a skeleton so cart flows can navigate without dead links."
		/>
	)
}
