import { RouteSkeleton } from "@/components/global/route-skeleton"

export default function WishlistPage() {
	return (
		<RouteSkeleton
			title="Wishlist"
			description="Wishlist route is scaffolded and ready for saved-item integration."
		/>
	)
}
