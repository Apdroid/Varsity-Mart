import { RouteSkeleton } from "@/components/global/route-skeleton"

export default function BecomeSellerPage() {
	return (
		<RouteSkeleton
			title="Become a Seller"
			description="Seller application screen is scaffolded for KYC submission, document upload, and approval flow."
		/>
	)
}
