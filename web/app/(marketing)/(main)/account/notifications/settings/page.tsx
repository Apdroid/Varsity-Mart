import { RouteSkeleton } from "@/components/global/route-skeleton"

export default function NotificationSettingsPage() {
	return (
		<RouteSkeleton
			title="Notification Settings"
			description="Notification preferences screen is scaffolded for channel controls and alert management."
		/>
	)
}
